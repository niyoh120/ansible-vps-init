- name: Set sysctl parameters
  ansible.posix.sysctl:
    name: "{{ item.key }}"
    value: "{{ item.value }}"
    state: present
    sysctl_set: yes # Ensure sysctl settings persist after reboot
    sysctl_file: "99-ansible-vps-init.conf"
    reload: yes
  with_dict: "{{ sysctl_config }}"
  tags:
    - sysctl
